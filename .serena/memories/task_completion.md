# 任务完成后的检查建议
- 若修改了文档内容或配置，优先运行 `npm run docs:build`，确认站点可以成功构建。
- 如需本地人工检查页面效果，运行 `npm run docs:dev`。
- 当前仓库未提供自动化测试、lint、format 脚本，因此主要验收手段是构建成功与页面人工预览。
- 如果后续引入真实业务文档，建议补充 README、依赖声明、lint/format/test 脚本，以提升可维护性。